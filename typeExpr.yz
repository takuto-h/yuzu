
rec:
  type t:
    pos : Pos.t
    raw : raw
  type raw:
    def Con(Names.typector)
    def Var(string)
    def App(Names.typector * list(t))
    def Tuple(list(t))
    def Fun(t * t)

def at(pos, raw): {
  pos = pos;
  raw = raw;
}
