
open Printf

def main():
  if Array.length(Sys.argv) = 1:
    eprintf("usage: %s files\n", Array.get(Sys.argv, 0))
  else:
    var fnames = Array.to_list(Array.sub(Sys.argv, 1, Array.length(Sys.argv) - 1))
    if Compiler.compile(fnames):
      exit(0)
    else:
      exit(1)

main()
