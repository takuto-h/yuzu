
open Printf

def main():
  if Array.length(Sys.argv) <> 3:
    eprintf("usage: %s in.yz out.ml\n", Array.get(Sys.argv, 0))
  else:
    if Trans.translate_file(Array.get(Sys.argv, 1), Array.get(Sys.argv, 2)):
      exit(0)
    else:
      exit(1)

main()
